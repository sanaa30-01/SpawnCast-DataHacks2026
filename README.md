# SpawnCast-DataHacks2026

FastAPI app and data live in **`backend/`**. From there: `uvicorn main:app --reload`.

Frontend (Vite) lives in **`frontend/`**. From there: `npm install`, then `npm run dev` (set `VITE_MAPBOX_TOKEN` in `frontend/.env`; see `frontend/.env.example`).
